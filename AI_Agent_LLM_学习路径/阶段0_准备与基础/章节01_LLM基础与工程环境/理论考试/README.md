# 理论考试

请用自己的话作答，不要只写定义。

1. 解释预训练、指令微调、对齐三者的目标分别是什么。
2. 为什么 token 不是“一个汉字等于一个 token”？
3. `temperature` 和 `top-p` 分别控制什么？它们能否互相替代？
4. 为什么说 hallucination 不是简单的“模型撒谎”？
5. 在什么情况下你会优先选用工具调用，而不是让模型直接回答？
6. Base Model 和 Chat Model 在工程使用上的差异是什么？
7. 如果用户要求高稳定性输出，你会从哪些参数和流程入手？
8. 解释为什么“API 能调通”不代表“产品能上线”。
